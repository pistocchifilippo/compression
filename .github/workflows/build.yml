name: Multiplatform build
on:
  push:
  schedule:
    - cron: '0 4 * * SUN'

jobs:
  compileAndTest:
    strategy:
      matrix:
        os: [ubuntu, macos, windows]
    runs-on: ${{ matrix.os }}-latest
    steps:
      - uses: actions/checkout@v2
      - id: compile
        uses: pistocchifilippo/build_test_java@master
        with:
          operation: 'BUILD'
      - run: echo ciao #./gradlew compileTestScala
  deploy:
    runs-on: ubuntu-latest
    needs: compileAndTest
    steps:
      - run: echo Deploy starts